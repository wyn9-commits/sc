DAX query to execute against the Sites and Costs Summary semantic model. The agent generates this query based on the user question using patterns from the knowledge file. Always start with EVALUATE. Use CALCULATE() for KeyMeasures. Use CALCULATE(SUM('CurMonData'[column])) for CurMonData aggregation columns including all Prior Year expenses and ROU projections. Use SUMMARIZECOLUMNS for grouped results. Use FILTER for row-level lookups. Use CALCULATETABLE for filtered table results. Never show this query text to the user.
